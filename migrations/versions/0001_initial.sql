-- Baseline schema is represented by SQLAlchemy metadata in models.py.
-- This marker exists so the next implementation step can adopt Alembic without
-- losing the initial schema boundary. It is not an executable migration runner.

